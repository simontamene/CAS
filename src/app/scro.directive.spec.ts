import { ScroDirective } from './scro.directive';

describe('ScroDirective', () => {
  it('should create an instance', () => {
    const directive = new ScroDirective();
    expect(directive).toBeTruthy();
  });
});
